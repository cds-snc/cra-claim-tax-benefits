extends ../base

block variables
  -var title = __('Medical costs')
  -var medicalAmount = hasData(data, 'deductions.medicalExpenseClaimAmount.amount') && data.deductions.medicalExpenseClaimAmount.amount !== 0 ? data.deductions.medicalExpenseClaimAmount.amount : ''

block content

  h1 #{title}
  
  div
    p #{__('You may include medical costs for:')}
    ul
      li #{__('Yourself')}
      li #{__('Your marriage or common law partner')}
      li #{__('You or your partner’s children, ')} <a href="/deductions/medical">#{__('if you are able')}</a> #{__('to claim their costs')}

  form.cra-form(method='post')
    +textInput('Enter total medical costs')(class='w-1-2 input-with-unit' name='medicalAmount' placeholder='0.00' aria-labelledby='medicalAmount__unit medicalAmount__label' value=medicalAmount)

    input#redirect(name='redirect', type='hidden', value='/deductions/political')

    div
      p #{__('For more information, contact CRA by:')}
      include ../_includes/phone-bullets
      
    +formButtons('Continue', '/deductions/medical')
