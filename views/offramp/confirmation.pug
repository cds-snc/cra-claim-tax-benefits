extends ../base

block variables
  -var title = __('You have reached the end of the prototype')

block content

  h1 #{title}
 
  div
    p #{__('To continue the survey')} <strong>#{__('click anywhere outside this popup window.')}</strong>
    p #{__('Clicking')} <i>#{__('Next')}</i> #{__(` once you are back in the survey, will proceed to the next step, where we'll ask for feedback on your experience.`)}

    

